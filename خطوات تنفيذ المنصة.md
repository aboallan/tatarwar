# دليل خطوة بخطوة لبناء منصة رسالة الاجتماع

> **نقطة البداية:** اعمل على الملفات الموجودة في جذر المشروع الحالي (`index.php`, `styles.css`, `script.js`, `auth.php`, `config.php`, `database.sql`). لا تبدأ بمشروع جديد؛ كل شيء مجهز في هذه الملفات لتعديله وتحسينه.

## 1. تهيئة قاعدة البيانات (SQL)
1. افتح ملف `database.sql` وراجع هيكل جدول `meeting_users` الحالي لتتأكد من الحقول المطلوبة للحضور، القسم، ملخص الاجتماع، ورمز الاعتماد الفريد.
2. أنشئ قاعدة بيانات جديدة في خادم MySQL لديك (مثال: `meeting_portal`).
3. نفّذ أوامر الملف `database.sql` داخل قاعدة البيانات الجديدة لإنشاء الجدول. يمكنك استخدام phpMyAdmin أو الأمر `mysql` من الطرفية.
4. أضف أي فهارس أو تعديلات تحتاجها لاحقًا (مثل جعل البريد الإلكتروني فريدًا) قبل الانتقال للواجهة.

## 2. إعداد الاتصال الخلفي (PHP)
1. حرّر ملف `config.php` وضع بيانات اتصال قاعدة البيانات (`DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS`).
2. تأكد من أن دالة `get_pdo()` تعيد اتصالاً صحيحًا (يمكنك اختبار ذلك بتشغيل `php -l config.php` ثم سكربت قصير يستدعي الدالة).
3. في ملف `auth.php` راجع الدوال المسؤولة عن التسجيل وتسجيل الدخول:
   - دالة تسجيل المستخدم الجديد تقوم بالتحقق من الحقول، إنشاء الرمز الفريد، وتخزين الحالة الأولية للحضور.
   - دالة تسجيل الدخول تتحقق من البريد وكلمة المرور وتعيد بيانات المستخدم للجلسة.
4. أضف سجلات تتبع (`error_log`) مؤقتة أثناء التطوير للتأكد من أن الطلبات تصل وتتعامل مع الأخطاء.

## 3. فهم الواجهة الحالية (HTML + PHP)
1. ملف `index.php` يحتوي على كل مكونات الواجهة. استخدم التعليقات الموجودة لتحديد الأقسام:
   - ترويسة المنصة وبطاقات الإحصاءات اليومية.
   - جدول أقسام اليوم ولوحة الحضور.
   - نموذج تسجيل الدخول والتسجيل مع التبويب المزدوج.
2. قبل التعديل، انسخ البنية الرئيسية لكل قسم حتى تتمكن من الرجوع إليها إذا احتجت.
3. خصص الأقسام المطلوبة (قائمة الحضور/الاعتذار، مواقع الاجتماع، تحديد هل كل الأقسام مدعوة أو قسم محدد، ملخص الاجتماع) داخل نفس المكوّنات الموجودة.

## 4. تخصيص الشكل (CSS)
1. افتح `styles.css` وحدد أقسام التعليقات لكل مجموعة (مثل `.app`, `.agenda`, `.auth-card`).
2. إذا رغبت بتغيير الألوان أو الخطوط، عدل المتغيرات المعرّفة داخل العنصر `:root` حتى تنعكس على كامل الواجهة.
3. حافظ على التدرج الزجاجي (glassmorphism) الموجود بإبقاء خصائص مثل `backdrop-filter` و`rgba` شبه شفافة. عدّل القيم تدريجيًا وتحقق من النتيجة في المتصفح.
4. اختبر التعديلات كل مرة بتحديث الصفحة، واستفد من أداة DevTools لمراقبة أحجام العناصر في اللغة العربية (اتجاه `rtl`).

## 5. التفاعلات الأمامية (JavaScript)
1. ملف `script.js` مسؤول عن التبديل بين تبويبي الدخول والتسجيل، والتحقق من توافق كلمة المرور.
2. إذا أضفت حقولًا جديدة في النماذج (مثل اختيار القسم أو ملخص الاجتماع)، تأكد من تعديل البرمجة لتحديث العدّادات أو الرسائل الفورية.
3. احرص على أن أي حدث `submit` يمنع الإرسال الافتراضي فقط عند وجود أخطاء؛ خلاف ذلك اتركه يرسل الطلب إلى `auth.php`.

## 6. اختبار الرحلة كاملة
1. شغّل خادم PHP محليًا عبر الأمر `php -S localhost:8000` من نفس المجلد.
2. جرّب التسجيل بمستخدم جديد وتأكد من إدراج السجل في قاعدة البيانات.
3. سجّل الدخول بالبيانات نفسها وراقب أن الجلسة تحتفظ بخيارات الحضور والموقع.
4. حرر السجلات يدويًا من قاعدة البيانات للتأكد من أن التعديلات تنعكس على الواجهة (مثل تغيير حالة الحضور إلى `decline`).

## 7. تحسينات مستقبلية مقترحة
- ربط لوحة الحضور في الواجهة ببيانات حقيقية من قاعدة البيانات بدل البيانات الوهمية.
- إضافة لوحة تحكم للمسؤول لتأكيد حضور الأقسام وكتابة الملاحظات العامة.
- إنشاء نظام إشعارات بريدية يعتمد على حقل `unique_code` لكل مستخدم لإرسال الدعوات ومتابعة الردود.

باتّباع الخطوات أعلاه ستعرف من أين تبدأ (ملفات المشروع الحالية) وكيف تنتقل تدريجيًا من إعداد القاعدة إلى تخصيص الواجهة والتجربة الكاملة.
